/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    remotePatterns: [
      {
        hostname: 'warmhearted-ant-59.convex.cloud',
      }
    ]
  }
};

export default nextConfig;
